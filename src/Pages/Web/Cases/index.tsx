import { Typography } from '@material-ui/core';
import React from 'react';
import { Container } from './styles';
const Cases: React.FC = () => {
  return (
    <Container>
      <Typography>Hello from Cases</Typography>
    </Container>
  );
};

export default Cases;
